---
dependencies:
  override:
    - echo 1
test:
  override:
    - echo 1
deployment:
  heroku:
    branch: circle
    owner: heroku
    commands:
      - heroku version
      - heroku update
      - heroku version
